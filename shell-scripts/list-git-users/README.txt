List git users with read permission in a organization
-----------------------------------------------------
- For testing create a organization in git and collaborators can be added.

- before executing export the username and token in cmd
	eg: export username=username
	    export token=token

- then execute the script list-users.sh
	- arg1 owner_name (organization name)
	- repo name

- it will return the user with pull access